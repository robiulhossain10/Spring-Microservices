server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-username/config-repo   # এখানে তোমার config রাখবে
          clone-on-start: true
